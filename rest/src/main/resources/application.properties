# App Properties
telemed.rest.jwtSecret= brainstormers
telemed.rest.jwtExpirationMs= 86400000

#Spring Config
spring.servlet.multipart.max-file-size=14MB
spring.servlet.multipart.max-request-size=14MB
spring.servlet.multipart.enabled=true

#Mongo Config:
#Mongo Docker container:
#spring.data.mongodb.host=airdocdb

#microservice configuration
spring.application.name=rest-service
server.port=8080
eureka.client.service-url.default-zone=http://localhost:8761/eureka
